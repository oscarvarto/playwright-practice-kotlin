"""Dashboard page implementations.

Each submodule exposes a single ``render()`` function that draws one page
of the Streamlit application.  The entry point in ``app.py`` lazily
imports and calls the appropriate ``render()`` based on the sidebar
selection.
"""
